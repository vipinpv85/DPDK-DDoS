incdir = include_directories('include')

executables = ['ddos', 'capture']

foreach ex:executables
	message('build for "' + ex + '"')
	message(' ------------------------------ ')
	subdir(ex)
endforeach
